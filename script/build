#!/bin/bash -e
#
# script/build
# mas
#
# Builds the Swift Package.
#

echo "==> 🏗️ Building mas ($(script/version))"
swift build \
  --configuration release \
  --arch arm64 \
  --arch x86_64 \
  --disable-sandbox \
  --manifest-cache none
