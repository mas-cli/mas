#!/bin/zsh -Ndefgku
#
# Scripts/clean
# mas
#
# Deletes the build directory & other generated files.
#

. "${0:a:h}/_setup_script"

print_notice '🗑​ Cleaning' "$(Scripts/version)" "${@}"

swift package clean
swift package reset
